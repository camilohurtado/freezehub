# infra

Terraform for the AWS deployment target. Not yet initialized.

Built minimally and just-in-time — see `FZ-063` in `../docs/08-backlog.md` and `../docs/02-architecture.md`. Do not build production infrastructure before the application needs it.
