terraform.tfvars
.terraform/
*.tfstate
*.tfstate.*
crash.log

# .terraform.lock.hcl is deliberately NOT ignored: it pins provider hashes, so an apply
# from CI resolves the same versions a developer verified.
