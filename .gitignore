# OS
.DS_Store

# Backend (Maven)
backend/target/

# IDE
.idea/
*.iml
.vscode/

# Design hand-offs: reference material, not source. The change set they carry is applied
# to frontend/src and reviewed there; keeping a second copy in the repo invites edits to
# the copy nobody builds.
design_handoff_*/
*.zip
