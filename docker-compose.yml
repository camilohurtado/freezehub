services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: freezehub
      POSTGRES_USER: freezehub
      POSTGRES_PASSWORD: freezehub
    ports:
      - "5432:5432"
    volumes:
      - freezehub-postgres-data:/var/lib/postgresql/data

volumes:
  freezehub-postgres-data:
